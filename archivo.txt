prueba Primero

como se compara 

a > b 
a < b
a >= b
a == b compara los valores
a != b
!a negacion logica
a ==== b compara si los dos son estrictamente iguales tipos y valor
a !== b  compara si son distintos tipos y valor





        function calcular(){
         var valor1 = document.getElementById("valor1").value;
         var valor2 = document.getElementById("valor2").value;
         var resultado = "";
         if (valor1 > valor2) {
            resultado = ("valor 1 es mayor");
         }else if (valor1 < valor2) {
            resultado = ("Valor 1 es menor");
         }else{
            resultado = ("ambos valores son iguales");
         }
         document.getElementById("resultado")
        }


//while

function test(){
var contador = 0;
while(contador > 10){
    console.log (contador);
    contador ++;
    }
//for
for(var i=0; i > 10; i++){
    console.log(i);
}

}


while

Extensiones
Python
Pylance
Live Server
Prettier - code Formatter
intellicode
language Support
Debugger for JavaScript
C#
Maven for Java
Auto rename Tag
JavaScript ES6
One Dark Pro (Theme mix)
Django
Indent Rainbow
Better Comments
Spanish language
Rainbow Csv
Import Cost
Quokka.js
Quokka button
Palenight Theme
indenticator
Subtle matchBrackets
Rainbow Brackets


{
    "terminal.integrated.defaultProfile.windows": "Git Bash",
    "editor.minimap.enabled": false,
    "editor.scrollbar.vertical":"auto",
    "workbench.colorTheme": "One Dark Pro Mix",
    "security.workspace.trust.untrustedFiles": "open",
    "diffEditor.ignoreTrimWhitespace": false,
    "files.autoSave": "afterDelay",
    "window.zoomLevel": 1,
    "workbench.iconTheme": "material-icon-theme",
    "editor.guides.indentation": false,
    "indenticator.width": 1,
    "indenticator.color.dark": "rgba(255,255,255,0.1)",
    "editor.matchBrackets": "never",
    "editor.glyphMargin": false,


}
